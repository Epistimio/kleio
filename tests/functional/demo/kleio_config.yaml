name: voila_voici

pool_size: 1
max_trials: 100

algorithms:
  gradient_descent:
    learning_rate: 0.1
    # dx_tolerance: 1e-7

database:
  type: 'mongodb'
  name: 'kleio_test'
  host: 'mongodb://user:pass@localhost'
